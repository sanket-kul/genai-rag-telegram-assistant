version: "3.9"

services:
  genai-rag-bot:
    build: .
    container_name: genai-rag-bot

    ports:
      - "8000:8000"
      - "7860:7860"

    env_file:
      - .env

    volumes:
      - .:/app

    restart: unless-stopped